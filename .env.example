# AI Hair Salon Agent local configuration.
# Copy to .env for local development. Do not commit .env.

# Real providers are allowed by default for backward-compatible local runtime.
# Tests, CI, and isolated validation explicitly override this value to deny.
EXTERNAL_CALL_POLICY=allow

# Database
DATABASE_URL=sqlite:///./data/salon.db

# OpenAI-compatible chat model used for classification and answer generation.
# Qwen DashScope compatible mode is one supported option.
# Fill LLM_API_KEY locally before real chat/provider calls. Do not commit it.
MODEL_PROVIDER=qwen
LLM_API_KEY=
LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_MODEL=qwen-plus

# Optional embedding config for code paths that need an OpenAI-compatible embedding provider.
# Fill EMBEDDING_API_KEY locally before real embedding/provider calls. Do not commit it.
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
EMBEDDING_MODEL=text-embedding-v4

# MCP Knowledge Service integration.
# Default is booking-only local startup.
# Set true only after RAG_MCP_SERVER_PYTHON and RAG_MCP_SERVER_CWD point to a valid local MCP Knowledge Service checkout.
# 只有在 RAG_MCP_SERVER_PYTHON 和 RAG_MCP_SERVER_CWD 已指向有效本地 MCP Knowledge Service 后，才设为 true。
RAG_MCP_ENABLED=false
RAG_MCP_SERVER_PYTHON=<PATH_TO_MCP_KNOWLEDGE_SERVICE>/.venv/bin/python
RAG_MCP_SERVER_MODULE=src.mcp_server.server
RAG_MCP_SERVER_CWD=<PATH_TO_MCP_KNOWLEDGE_SERVICE>
RAG_MCP_COLLECTION=salon_knowledge
RAG_MCP_QUERY_TOP_K=4

# CORS.
# Empty by default: same-origin usage only, no cross-origin middleware.
# Use comma-separated explicit origins only when a separate frontend origin is needed. Do not use wildcard.
CORS_ALLOWED_ORIGINS=

# Optional account authentication. Guest booking remains available when disabled.
# Generate a private value of at least 32 random bytes for AUTH_JWT_SECRET.
AUTH_ENABLED=true
AUTH_JWT_SECRET=
AUTH_JWT_ALGORITHM=HS256
AUTH_ACCESS_TOKEN_MINUTES=15
AUTH_REFRESH_TOKEN_DAYS=30
AUTH_JWT_ISSUER=ai-hair-salon-agent
AUTH_JWT_AUDIENCE=ai-hair-salon-web
AUTH_COOKIE_NAME=salon_access_token
AUTH_REFRESH_COOKIE_NAME=salon_refresh_token
AUTH_REFRESH_COOKIE_PATH=/api/auth
AUTH_CSRF_COOKIE_NAME=salon_csrf_token
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAMESITE=lax
AUTH_REFRESH_REUSE_GRACE_SECONDS=3
AUTH_AUTH_SESSION_RETENTION_DAYS=30

# 登录和注册使用当前应用进程内的有界滑动窗口限流。
# 仅识别直连 request.client.host，不信任客户端提交的代理转发头。
AUTH_RATE_LIMIT_ENABLED=true
AUTH_LOGIN_CLIENT_LIMIT=10
AUTH_LOGIN_CLIENT_WINDOW_SECONDS=60
AUTH_LOGIN_CLIENT_ACCOUNT_LIMIT=5
AUTH_LOGIN_CLIENT_ACCOUNT_WINDOW_SECONDS=300
AUTH_REGISTER_CLIENT_LIMIT=3
AUTH_REGISTER_CLIENT_WINDOW_SECONDS=3600
AUTH_REFRESH_CLIENT_LIMIT=30
AUTH_REFRESH_CLIENT_WINDOW_SECONDS=60
AUTH_RATE_LIMIT_MAX_BUCKETS=10000
AUTH_RATE_LIMIT_CLEANUP_INTERVAL_SECONDS=60

# Optional Weather Context Tool.
# Open-Meteo requires no API key and runs only after a conversational booking is saved.
WEATHER_ENABLED=true
WEATHER_PROVIDER=open_meteo
WEATHER_LOCATION_NAME=上海
WEATHER_LATITUDE=31.2304
WEATHER_LONGITUDE=121.4737
WEATHER_TIMEZONE=Asia/Shanghai
WEATHER_TIMEOUT_SECONDS=3
WEATHER_FORECAST_DAYS=16
